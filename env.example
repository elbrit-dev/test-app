# Plasmic Version Control Configuration
# Copy this file to .env.local and set your version tag

# Use 'dev' for development, 'prod' for production
# Leave empty or omit to always fetch the latest version
PLASMIC_VERSION_TAG=dev

# ==============================================================================
# Novu Configuration (for Push Notifications)
# ==============================================================================
# Get your Novu Secret Key from: https://web.novu.co/settings/api-keys
# IMPORTANT: Use NOVU_SECRET_KEY (without NEXT_PUBLIC_) for server-side API routes
# This keeps your secret key secure and not exposed to the client
NOVU_SECRET_KEY=your_novu_secret_key_here

# Novu Application Identifier (for Inbox component)
# Get this from: https://web.novu.co/settings/environments
# This is safe to expose to the client (starts with NEXT_PUBLIC_)
NEXT_PUBLIC_NOVU_APPLICATION_IDENTIFIER=your_application_identifier_here

# Optional: Integration Identifier for OneSignal
# Only needed if you have multiple OneSignal integrations in Novu
# Leave empty if you only have one OneSignal integration
NOVU_INTEGRATION_IDENTIFIER=

# Optional: EU Region URLs (only needed if your Novu account is in EU region)
# NEXT_PUBLIC_NOVU_BACKEND_URL=https://eu.api.novu.co
# NEXT_PUBLIC_NOVU_SOCKET_URL=wss://eu.socket.novu.co

# ==============================================================================
# SETUP INSTRUCTIONS:
# ==============================================================================
# 1. Copy this file:
#    cp env.example .env.local
#
# 2. For development environment, create .env.development:
#    echo "PLASMIC_VERSION_TAG=dev" > .env.development
#
# 3. For production environment, create .env.production:
#    echo "PLASMIC_VERSION_TAG=prod" > .env.production
#
# ==============================================================================
# HOW IT WORKS:
# ==============================================================================
# - Next.js automatically loads .env files based on NODE_ENV
# - In development: .env.development is used
# - In production: .env.production is used
# - .env.local always overrides other .env files (except .env.*.local)
#
# ==============================================================================
# VERSION TAGS:
# ==============================================================================
# Version tags let you pin your app to specific Plasmic versions:
# - 'dev': Development version with latest changes
# - 'prod': Stable production version
# - undefined/empty: Always fetch the latest published version
#
# To create version tags in Plasmic:
# 1. Go to your Plasmic project
# 2. Click on "Publish" → "Version tags"
# 3. Create tags named 'dev' and 'prod'
# 4. Assign versions to these tags
